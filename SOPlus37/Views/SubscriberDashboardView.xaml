<UserControl x:Class="SOPlus37.Views.SubscriberDashboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="Transparent">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0"
                Background="#FFF7ED"
                BorderBrush="#FED7AA"
                BorderThickness="1"
                CornerRadius="12"
                Padding="14"
                Margin="0,0,0,12">
            <Border.Style>
                <Style TargetType="Border">
                    <Setter Property="Visibility" Value="Collapsed"/>
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding IsLoading}" Value="True">
                            <Setter Property="Visibility" Value="Visible"/>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </Border.Style>

            <TextBlock Text="Загрузка данных..."
                       Foreground="#9A3412"
                       FontSize="13"/>
        </Border>

        <Border Grid.Row="1"
                Background="#FEF2F2"
                BorderBrush="#FECACA"
                BorderThickness="1"
                CornerRadius="12"
                Padding="14"
                Margin="0,0,0,12">
            <Border.Style>
                <Style TargetType="Border">
                    <Setter Property="Visibility" Value="Collapsed"/>
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding Error}" Value="{x:Null}">
                            <Setter Property="Visibility" Value="Collapsed"/>
                        </DataTrigger>
                        <DataTrigger Binding="{Binding Error}" Value="">
                            <Setter Property="Visibility" Value="Collapsed"/>
                        </DataTrigger>
                        <DataTrigger Binding="{Binding Error}" Value="{x:Null}">
                            <Setter Property="Visibility" Value="Collapsed"/>
                        </DataTrigger>
                        <Trigger Property="Visibility" Value="Collapsed"/>
                    </Style.Triggers>
                </Style>
            </Border.Style>

            <TextBlock Text="{Binding Error}"
                       Foreground="#DC2626"
                       FontSize="13"
                       TextWrapping="Wrap"/>
        </Border>

        <Border Grid.Row="2"
                Background="White"
                BorderBrush="#E7ECF5"
                BorderThickness="1"
                CornerRadius="16"
                Padding="18">

            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="220"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Row="0" Grid.ColumnSpan="2"
                           Text="Информация об абоненте"
                           FontSize="18"
                           FontWeight="Bold"
                           Foreground="#0F172A"
                           Margin="0,0,0,14"/>

                <TextBlock Grid.Row="1" Grid.Column="0"
                           Text="Номер телефона"
                           Foreground="#64748B"
                           FontSize="13"
                           Margin="0,0,0,10"/>
                <TextBlock Grid.Row="1" Grid.Column="1"
                           Text="{Binding Info.PhoneNumber}"
                           Foreground="#0F172A"
                           FontSize="14"
                           FontWeight="SemiBold"
                           Margin="0,0,0,10"/>

                <TextBlock Grid.Row="2" Grid.Column="0"
                           Text="Статус"
                           Foreground="#64748B"
                           FontSize="13"
                           Margin="0,0,0,10"/>
                <TextBlock Grid.Row="2" Grid.Column="1"
                           Text="{Binding Info.Status}"
                           Foreground="#0F172A"
                           FontSize="14"
                           FontWeight="SemiBold"
                           Margin="0,0,0,10"/>

                <TextBlock Grid.Row="3" Grid.Column="0"
                           Text="Тип клиента"
                           Foreground="#64748B"
                           FontSize="13"
                           Margin="0,0,0,10"/>
                <TextBlock Grid.Row="3" Grid.Column="1"
                           Text="{Binding Info.ClientType}"
                           Foreground="#0F172A"
                           FontSize="14"
                           FontWeight="SemiBold"
                           Margin="0,0,0,10"/>

                <TextBlock Grid.Row="4" Grid.Column="0"
                           Text="ФИО / Организация"
                           Foreground="#64748B"
                           FontSize="13"/>
                <StackPanel Grid.Row="4" Grid.Column="1">
                    <TextBlock Text="{Binding Info.DisplayName}"
                               Foreground="#0F172A"
                               FontSize="14"
                               FontWeight="SemiBold"/>
                    <TextBlock Text="{Binding Info.IdentityDocument}"
                               Foreground="#64748B"
                               FontSize="12"
                               Margin="0,6,0,0"/>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</UserControl>
